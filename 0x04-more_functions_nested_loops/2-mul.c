#include "main.h"

/**
 * mul - multiplies two numbers and return the value
 * @num1: first variable
 * @num2: second variable
 * Return: value after multiplication
 */

int mul(int num1, int num2)
{
	int value;

	value = num1 * num2;
	return (value);
}
